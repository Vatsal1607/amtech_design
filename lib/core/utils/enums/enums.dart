enum DetailsType {
  details,
  subscription,
}

enum AddressType {
  Home,
  Work,
  Other,
}

enum HomeAddressType {
  local,
  remote,
}

enum PaymentType {
  order,
  recharge,
  subscription,
}

enum SelectedPaymentMethod {
  perks,
  upi,
}
